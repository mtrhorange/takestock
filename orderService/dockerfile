# === Dockerfile for Backend Services (config-server, service-registry, userService, productService, orderService) ===
# Save this as Dockerfile in each backend service folder
FROM maven:3.9.6-eclipse-temurin-17 AS build
WORKDIR /app
COPY . .
RUN mvn clean package -DskipTests

# Stage 2: Copy built JAR into a lightweight image
FROM openjdk:17-jdk-slim
COPY --from=build /app/target/*.jar app.jar
ENTRYPOINT ["java", "-jar", "/app.jar"]